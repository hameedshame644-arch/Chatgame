# ChatVerse – Full Project (React + Supabase PostgreSQL)

Human-moderated justice-based chat platform.

## Stack
- React (Vite-style)
- Supabase (PostgreSQL, Auth, Realtime)
- SQL-only business logic (no AI, no backend server)

## Setup
1. Create Supabase project
2. Run SQL files in order (supabase/sql)
3. Add env vars in frontend
4. Run React app
